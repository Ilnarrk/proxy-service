# Host and port the proxy listens on
PROXY_HOST=0.0.0.0
PROXY_PORT=8080

# Target server to forward requests to
TARGET_HOST=http://localhost:9000

# Separate target server for POST /soap/* requests
SOAP_TARGET_HOST=http://localhost:9001
